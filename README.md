# git tutorial

### basic command
  - git init (to initialize git in a folder -> repository)
  - git add
  - git commit -m "commit message"

-----

### function 'greet()' -> greeting people
- parameter : name (name of the person)
- parameter : greeting (default value = 'Hello)